# Real FotMob player-context authoritative team-strength bridge

## Boundary

PR #197 closes the authority gap left deliberately by PR #194 for the exact real Nottingham Forest vs Leeds United player-context observation.

PR #194 proved that the exact PR #193 admission maps into the PR #190 team-strength candidate with exactly two AVAILABLE current-context resolutions:

- Nottingham Forest unavailable-player count = `1`;
- Leeds United unavailable-player count = `5`.

PR #194 correctly kept `team_strength_feature_authorized = false` because that real observation had not yet been carried through admitted PR #65 Fixture Intelligence / PR #66 model-feature ancestry.

PR #197 adds only that missing ancestry and then routes the exact candidate into ATHENA's **existing PR #191 `ReviewedFotMobTeamStrengthContext` authority type**.

There are deliberately two different objects with different jobs:

1. the PR #197 same-raw PR #65/PR #66 prerequisite bridge, whose authority map is **all false**;
2. the existing PR #191 wrapper, whose safety map alone may set `team_strength_feature_authorized = true`.

The prerequisite bridge is evidence proof, not a second authority dialect.

PR #197 does not add a player-impact model, expected-goals coefficient, probability, bookmaker price, selection, production approval, or BET authority.

## Exact real source

The boundary accepts only the five frozen PR #192 evidence byte streams:

1. `campaign-receipt.json`;
2. `match-details/manifest.json`;
3. `match-details/response.json`;
4. `match-details/persisted-evidence-receipt.json`;
5. `match-details/structure-assessment.json`.

Those bytes are the exact successful prospective capture for:

- fixture `FOTMOB:5795367`;
- Nottingham Forest vs Leeds United;
- kickoff `2026-08-22T14:00:00Z`;
- raw SHA-256 `7b6fe187ae3dd175721f51be107f822a89359f8c6891854f4035b07b449a8e99`;
- PR #53 structure SHA-256 `8ac7b767caedf427e32c142ed91dd71ab2bd64444513f906ab949f88f361bcea`.

The hosted proof downloads the exact PR #192 artifact from run `32410775191`, artifact `9422055017`, source head `46f76e8033d3d498131c6f893111b437b6b459a9`.

No new FotMob request occurs in PR #197.

## Why the generic PR #191 array builder is not called directly

PR #191's generic repeated-player-array contract requires an explicitly reviewed source-side home/away Boolean through `is_home_pointer`.

The exact PR #192/PR #193 `content.lineup.homeTeam` and `content.lineup.awayTeam` objects do not contain such a Boolean. PR #193 established HOME/AWAY semantics from the exact fixture/team structure instead.

Inventing a Boolean merely to satisfy the generic array contract would weaken the source boundary.

PR #197 therefore preserves PR #191's **authority discipline** without forging the generic input type:

- PR #193 is fully replayed to establish exact player-array/team-side semantics;
- PR #194 is fully replayed to reconstruct the exact PR #190 candidate;
- the exact same raw SHA and PR #53 structure are independently carried through a complete admitted PR #54→PR #66 scalar lineage;
- the prerequisite bridge remains all-false;
- only after those source-replayed branches agree does the source-specific adapter construct the existing PR #191 authority type.

The final context's `source_array_artifact_sha256` / `source_array_artifact_size` slots bind the exact canonical PR #193 real-array admission artifact (`acf53d913ee3d7a6c4f357860aa2730b5122ad8a169f4a38bcc4ab882c6d4ad8`, `14089` bytes). They are not caller-provided identities.

The source-specific adapter itself accepts no candidate, model coefficient, probability, bookmaker, price, or historical-player input. It accepts only the same five exact PR #192 source byte streams and rebuilds the complete prerequisite before constructing the existing PR #191 type.

## The lineage sentinel

The exact raw observation already reviewed by PR #193 contains:

`/content/lineup/lineupType = "predicted"`

PR #197 uses that one scalar as a lineage sentinel:

- category: `LINEUP`;
- field: `source_lineup_type`;
- exact value: `predicted`.

The sentinel is explicitly reviewed, qualified only for this exact observation, freshness-bound only through PR #193's classification instant, materialized as one SUPPORTED Fixture Intelligence fact, admitted as the whole one-member candidate set, and carried through PR #65 and PR #66.

It is **not** a PR #31 model feature. The builder requires every generic PR #31 resolution produced from the sentinel-only snapshot to remain `MISSING`. If that changes, the bridge fails closed.

The sentinel exists only to prove that the exact PR #192 raw/PR #53 observation crossed the existing admitted Fixture Intelligence/model-feature ancestry.

## Candidate identity and feature availability

The nested PR #190 candidate remains byte-identical to PR #194's frozen candidate SHA:

`cc48bbcea5a17ff57a39cc951c5e69005008d857366359528aaf46f979c30745`

The only AVAILABLE team-strength candidate features remain:

- `home_unavailable_player_count = 1.0`;
- `away_unavailable_player_count = 5.0`.

No additional current, historical, lineup-depth, position, rating, rest, continuity, replacement-quality, or player-quality feature becomes AVAILABLE.

The prerequisite bridge's authority map remains all false. The final existing PR #191 wrapper may state:

`team_strength_feature_authorized = true`

for this exact source-replayed observation and classification instant only.

The nested PR #190 candidate safety map also remains all false, as designed.

## Predicted lineup and missing bench

PR #193 admits the `starters` arrays as an EXPECTED starting XI because source `lineupType` is exactly `predicted`.

The captured observation contains no reviewed bench/substitute root. PR #197 therefore preserves PR #194's aggregate team lineup state as:

`UNVERIFIED_LINEUP_STATE`

It does not infer an empty bench, complete squad depth, or confirmed XI.

## Position and player quality

PR #193 preserves source numeric position IDs and market-value fields but does not authorize their football/model meanings.

PR #197 does not introduce those semantics. Player components remain position `UNKNOWN`, source position is absent from the PR #190 candidate, and no market-value or star-player score is used.

## Historical evidence

No historical player starts, minutes, ratings, replacement performance, or complete schedule-history population is created by this boundary.

Historical player-dependent PR #190 features remain MISSING/BLOCKED.

## Freshness

PR #193 froze:

`STATE_FRESH_UNTIL = CLASSIFIED_AT = 2026-08-20T20:24:00Z`

PR #197 preserves that exact boundary and proves:

`prospective_reuse_after_source_freshness_authorized = false`

Therefore PR #197 proves that ATHENA can legally construct the existing PR #191 team-strength authority from a fresh reviewed real player-context observation when one exists. It does **not** claim that Thursday's observation remains current for Saturday.

The Saturday prospective pipeline must obtain and review a newer observation and pass the same authority discipline before current player context can be treated as current.

## Probability discipline

`team_strength_feature_authorized = true` does not mean an unavailable-player count may be translated into expected goals.

PR #197 grants no:

- probability inference;
- probability adjustment;
- expected-goals adjustment;
- calibration authority;
- pricing;
- selection;
- production approval;
- BET authority.

A later xG challenger may consume only context variables for which a training/evaluation mechanism is scientifically justified. There is no rule such as "five unavailable players = minus X expected goals".

## Hosted proof

The dedicated hosted proof must establish on the exact PR head:

- exact PR #192 artifact provenance;
- exact PR #193 semantic replay;
- exact PR #194 candidate replay;
- exact same raw SHA/PR #53 structure through the one-fact PR #65 snapshot;
- exact PR #66 handoff;
- zero AVAILABLE generic PR #31 model features from the lineage sentinel;
- all authority flags false on the prerequisite bridge;
- construction of the existing PR #191 `ReviewedFotMobTeamStrengthContext` type;
- exact PR #190 candidate SHA and only the two unavailable-count AVAILABLE resolutions;
- `team_strength_feature_authorized = true` only in the existing PR #191 wrapper;
- `prospective_reuse_after_source_freshness_authorized = false` for the source observation;
- probability/pricing/selection/BET authority false.

Failure of any source identity, hash, chronology, source-artifact, candidate, PR #65, PR #66, authority-map, or byte-revalidation condition fails closed.
