name: Verify Real FotMob Player-Context Authoritative Bridge

on:
  pull_request:
    types: [opened, synchronize, reopened, edited]

concurrency:
  group: athena-real-fotmob-player-context-authoritative-bridge-v1
  cancel-in-progress: false

jobs:
  verify:
    if: >-
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.event.pull_request.head.ref == 'model/fotmob-real-player-context-authoritative-bridge'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    permissions:
      actions: read
      contents: read
      pull-requests: read

    steps:
      - name: Validate exact PR197 source lineage
        id: guard
        uses: actions/github-script@f28e40c7f34bde8b3046d885e986cb6290c5673b
        with:
          script: |
            const pr = context.payload.pull_request;
            if (!pr) {
              core.setFailed('pull_request payload required');
              return;
            }
            if (pr.base.sha !== 'e7d4df4f82aee875eda6e72b5c3925b24b05c49f') {
              core.setFailed(`PR base ${pr.base.sha} differs from frozen PR196 merge base.`);
              return;
            }
            if (
              pr.head.repo.full_name !== context.repo.owner + '/' + context.repo.repo ||
              pr.head.ref !== 'model/fotmob-real-player-context-authoritative-bridge'
            ) {
              core.setFailed('exact same-repository PR197 branch binding failed');
              return;
            }

            const sourceArtifactId = 9422055017;
            const sourceRunId = 32410775191;
            const sourceHeadSha = '46f76e8033d3d498131c6f893111b437b6b459a9';
            const sourceDigest = 'sha256:db5dc12b8863cbac15f210e018ddf0af9b9011a6ad8c3958a473a597254f44b5';
            const { data: artifact } = await github.rest.actions.getArtifact({
              owner: context.repo.owner,
              repo: context.repo.repo,
              artifact_id: sourceArtifactId,
            });
            if (
              artifact.id !== sourceArtifactId ||
              artifact.name !== 'fotmob-prospective-player-context-evidence' ||
              artifact.expired ||
              artifact.size_in_bytes !== 974969 ||
              artifact.digest !== sourceDigest ||
              !artifact.workflow_run ||
              artifact.workflow_run.id !== sourceRunId ||
              artifact.workflow_run.head_sha !== sourceHeadSha
            ) {
              core.setFailed('exact PR192 source artifact identity/provenance mismatch');
              return;
            }
            const { data: run } = await github.rest.actions.getWorkflowRun({
              owner: context.repo.owner,
              repo: context.repo.repo,
              run_id: sourceRunId,
            });
            if (
              run.status !== 'completed' ||
              run.conclusion !== 'success' ||
              run.event !== 'pull_request' ||
              run.head_sha !== sourceHeadSha ||
              run.name !== 'Execute FotMob Prospective Player-Context Campaign'
            ) {
              core.setFailed('source workflow run is not the exact successful PR192 evidence run');
              return;
            }
            core.setOutput('head_sha', pr.head.sha);
            core.setOutput('artifact_id', String(sourceArtifactId));
            core.setOutput('run_id', String(sourceRunId));

      - name: Checkout exact PR head
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: ${{ steps.guard.outputs.head_sha }}
          fetch-depth: 0
          persist-credentials: false

      - name: Verify frozen ancestry and clean checkout
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "${{ steps.guard.outputs.head_sha }}"
          git merge-base --is-ancestor e7d4df4f82aee875eda6e72b5c3925b24b05c49f HEAD
          test -z "$(git status --short --untracked-files=no)"

      - name: Set up Python 3.12
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: requirements.txt

      - name: Install frozen repository dependencies
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt

      - name: Download exact successful PR192 evidence artifact
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          artifact-ids: ${{ steps.guard.outputs.artifact_id }}
          path: source-evidence
          github-token: ${{ github.token }}
          repository: ${{ github.repository }}
          run-id: ${{ steps.guard.outputs.run_id }}

      - name: Full-replay PR192 through PR66 and build authoritative bridge
        id: bridge
        env:
          PYTHONPATH: ${{ github.workspace }}
          EXACT_HEAD_SHA: ${{ steps.guard.outputs.head_sha }}
        shell: bash
        run: |
          set -euo pipefail
          python scripts/verify_fotmob_real_player_context_authoritative_bridge.py \
            --source-root source-evidence/fotmob-prospective-player-context-evidence \
            --output-root authoritative-bridge-proof \
            --repository-head-sha "${EXACT_HEAD_SHA}" \
            | tee authoritative-bridge-summary.txt
          bridge_sha="$(awk -F= '/^bridge_sha256=/{print $2}' authoritative-bridge-summary.txt)"
          test "${#bridge_sha}" -eq 64
          grep -Fx 'exact_pr192_pr193_pr194_replay_verified=true' authoritative-bridge-summary.txt
          grep -Fx 'exact_same_raw_pr53_pr65_pr66_lineage_verified=true' authoritative-bridge-summary.txt
          grep -Fx 'sentinel_pr65_fact_count=1' authoritative-bridge-summary.txt
          grep -Fx 'sentinel_pr31_available_feature_count=0' authoritative-bridge-summary.txt
          grep -Fx 'home_unavailable_player_count=1.0' authoritative-bridge-summary.txt
          grep -Fx 'away_unavailable_player_count=5.0' authoritative-bridge-summary.txt
          grep -Fx 'team_strength_feature_authorized=true' authoritative-bridge-summary.txt
          grep -Fx 'prospective_reuse_after_source_freshness_authorized=false' authoritative-bridge-summary.txt
          grep -Fx 'probability_pricing_selection_bet_authorized=false' authoritative-bridge-summary.txt
          echo "bridge_sha=${bridge_sha}" >> "${GITHUB_OUTPUT}"

      - name: Upload exact authoritative bridge proof
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: fotmob-real-player-context-authoritative-bridge
          path: authoritative-bridge-proof/
          if-no-files-found: error
          retention-days: 30
          compression-level: 0

      - name: Publish exact boundary summary
        shell: bash
        run: |
          set -euo pipefail
          {
            echo '## Real FotMob player-context authoritative team-strength bridge'
            echo
            echo "- exact PR head: ${{ steps.guard.outputs.head_sha }}"
            echo "- source PR192 run: ${{ steps.guard.outputs.run_id }}"
            echo "- source artifact id: ${{ steps.guard.outputs.artifact_id }}"
            echo "- bridge SHA-256: ${{ steps.bridge.outputs.bridge_sha }}"
            echo '- exact PR192 -> PR193 -> PR194 replay: VERIFIED'
            echo '- exact same raw/PR53 observation admitted through PR65/PR66: VERIFIED'
            echo '- lineage sentinel PR65 fact count: 1'
            echo '- lineage sentinel PR31 AVAILABLE feature count: 0'
            echo '- team-strength feature authority: TRUE for exact PR193 classification instant only'
            echo '- candidate AVAILABLE resolutions: home/away unavailable-player count only'
            echo '- aggregate lineup state: UNVERIFIED because bench root is missing'
            echo '- position semantics: NOT USED'
            echo '- historical player evidence: NOT USED'
            echo '- reuse after PR193 freshness instant: FALSE'
            echo '- probability/pricing/selection/BET authority: FALSE'
          } >> "${GITHUB_STEP_SUMMARY}"
